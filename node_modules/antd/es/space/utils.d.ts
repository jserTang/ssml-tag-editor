import type { SpaceSize } from '.';
import type { SizeType } from '../config-provider/SizeContext';
export declare function isPresetSize(size: SpaceSize): size is SizeType;
export declare function isValidGapNumber(size: SpaceSize): size is number;
